import Layout from "components/layout";
import { NextPage } from "next";

const Browsers: NextPage = () => {
  return <Layout page_name="Browsers">How do Browsers work?</Layout>;
};

export default Browsers;
